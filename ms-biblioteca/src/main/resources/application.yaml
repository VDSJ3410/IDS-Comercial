server:
  port: 8080

spring:
  application:
    name: ms-biblioteca

  datasource:
    url: jdbc:h2:mem:biblioteca
    driverClassName: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

